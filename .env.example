# Secretary daemon environment variables
# Copy to .env and fill in values

# LLM API key (used by Mem0 for extraction)
OPENAI_API_KEY=

# todo-for-ai auth token (if using authenticated mode)
TODO_API_TOKEN=

# OpenCode serve auth token (if enabled)
OPENCODE_TOKEN=
